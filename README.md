# teste

Modernised Flask service with structured blueprints and service layers.

## Getting started

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt  # create as needed
python run.py
```

Environment variables documented in `app/config.py` allow overriding API keys and storage paths.
